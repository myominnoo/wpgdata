% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peg_data.R
\name{peg_data}
\alias{peg_data}
\title{Fetch data from the Winnipeg Open Data Portal}
\usage{
peg_data(
  dataset_id,
  filter = NULL,
  select = NULL,
  top = NULL,
  skip = NULL,
  orderby = NULL,
  max_connections = NULL
)
}
\arguments{
\item{dataset_id}{A character string dataset ID e.g. \code{"d4mq-wa44"}.}

\item{filter}{A filter expression — either an R expression such as
\code{total_assessed_value > 500000}, or a raw OData string such as
\code{"total_assessed_value gt 500000"}. Use \code{\link[=peg_metadata]{peg_metadata()}} to look up
valid field names.}

\item{select}{A character vector of field names to return,
e.g. \code{c("roll_number", "total_assessed_value")}.}

\item{top}{A positive integer — the maximum number of rows to return.
When \code{NULL} (the default) every row is fetched.}

\item{skip}{A non-negative integer — rows to skip before collecting
results.}

\item{orderby}{A character string specifying sort order,
e.g. \code{"total_assessed_value desc"}.}

\item{max_connections}{A positive integer controlling how many HTTP
requests are in-flight at once. When \code{NULL} (the default), the value is
auto-detected as \code{2 * parallel::detectCores()}, capped at 10 and at the
number of pages required. Supply an explicit value to override — lower if
the server rate-limits, higher if you have headroom.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with the requested rows in their original order.
}
\description{
A unified replacement for \code{peg_get()}, \code{peg_query()}, and \code{peg_all()}.
Pages are fetched \strong{in parallel} for maximum throughput.
}
\details{
How it works:
\enumerate{
\item Fires the \verb{$count} request and the first data page \strong{simultaneously},
saving one full round trip before the main fetch begins.
\item Pre-computes every remaining page URL from \verb{$skip} offsets — no
\code{nextLink} chaining required.
\item Fires all remaining requests via \code{\link[httr2:req_perform_parallel]{httr2::req_perform_parallel()}} with
\code{max_active} throttling. curl starts the next request the instant any
slot frees, avoiding the convoy delay of manual batching.
\item Binds pages in their original order and trims to \code{top}.
}
}
\examples{
\dontrun{
# fetch every row
peg_data("d4mq-wa44")

# fetch the first 500 rows
peg_data("d4mq-wa44", top = 500)

# filter + select + sort, fetch all matching rows
peg_data(
  "d4mq-wa44",
  filter  = total_assessed_value > 500000,
  select  = c("roll_number", "total_assessed_value"),
  orderby = "total_assessed_value desc"
)

# override connection count
peg_data("d4mq-wa44", max_connections = 4L)
}
}
